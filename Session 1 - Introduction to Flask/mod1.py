import mod2 
# when a library is called it is executed so if you run mod1.py it will also execute mod2, 
# So it is better to use main function in each code so that it will not get executed

print(f"Running mod1 - ({__name__})")